spring.application.name=easy-finance
# URL de conexão com o banco de dados PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/easy_finance

# Nome de usuário do PostgreSQL
spring.datasource.username=postgres

# Senha do banco de dados PostgreSQL
spring.datasource.password=Viniciuscruz07

# Mostrar as consultas SQL no console
spring.jpa.show-sql=true

api.security.token.secret=${JWT_SECRET:my-secret-key}